[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hnb-venus-api-docs-unofficial"
version = "0.1.0"
description = "Minimal unofficial HTTP helpers for HNB Venus API (educational)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.urls]
Homepage = "https://github.com/Cookie-Cat21/hnb-venus-api-docs"

[tool.setuptools]
packages = ["hnb_venus_api_docs"]
