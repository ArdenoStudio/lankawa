[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nsb-rates-docs-unofficial"
version = "0.1.0"
description = "Minimal unofficial HTTP helpers for NSB Rates Pages (educational)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.urls]
Homepage = "https://github.com/Cookie-Cat21/nsb-rates-docs"

[tool.setuptools]
packages = ["nsb_rates_docs"]
