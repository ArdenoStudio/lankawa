# Pagination lab — Promise.lk tenders

Endpoints with `pagination.lab: true`:

## `procurements_list`

- **Style:** `html_list`
- **URL:** `https://promise.lk/`
- **Params:** `None`
- **Notes:** —

### curl (page / offset variants)

```bash
curl -sS -A 'LankawaApiDocsBot/1.0' 'https://promise.lk/' | head
```
