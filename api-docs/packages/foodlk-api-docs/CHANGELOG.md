# Changelog

Automated from `catalog/endpoints.yaml` via `scripts/update-api-catalog-changelog.py` (and `.github/workflows/catalog-changelog.yml` after extraction).

## 2026-07-20

- **Bootstrap** catalog fingerprint (4 endpoints).
- Listed `basket_estimate` — GET `/api/v1/basket/estimate` · access `json_api`
- Listed `hub_manifest` — GET `/api/v1/hub/manifest` · access `json_api`
- Listed `hub_summary` — GET `/api/v1/hub/summary` · access `json_api`
- Listed `openapi` — GET `/openapi.json` · access `json_api`
