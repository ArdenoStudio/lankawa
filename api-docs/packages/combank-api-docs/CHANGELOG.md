# Changelog

Automated from `catalog/endpoints.yaml` via `scripts/update-api-catalog-changelog.py` (and `.github/workflows/catalog-changelog.yml` after extraction).

## 2026-07-20

- **Bootstrap** catalog fingerprint (3 endpoints).
- Listed `exchange_rates` — GET `/api/exchange-rates` · access `json_api`
- Listed `interest_rates_fd` — GET `/api/interest-rates-fd` · access `json_api`
- Listed `rewards_promotions_html` — GET `/rewards-promotions` · access `html_scrape`
